# Breadth First Search(BFS)

```python

def tree_breadth_first(tree):
    queue = Queue()
    output_list=[]
    current = queue.front
    while(current):
        # enqueue the current
        # check if the current has a left (if true):
            # queue.enqueue(current.left)
        # check if the current has a right (if true):
            # queue.enqueue(current.right)
        
        # output_list.append(queue.dequeue())
    return output_list

```